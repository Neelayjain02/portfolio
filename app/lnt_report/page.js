// 1. Import the component (You can name it whatever you want here, but must be Capitalized)
import LnTReport from "../../components/lnt_report";

export default function Page() {
  // 2. Use it with an UPPERCASE letter
  return <LnTReport />;
}